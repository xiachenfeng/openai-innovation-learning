# First Session Prompt

```text
初始化这个 OpenAI 创新算法学习项目。

先只读检查：
- AGENTS.md
- curriculum.md
- timeline.md
- state/
- sources/

然后逐题采访我，评估 AGENTS.md Initialization 的 8 项清单：
1. Transformer decoder、next-token pretraining、fine-tuning；
2. Prompting、in-context learning、scaling laws；
3. PPO、reward model、RLHF（DPO 只作对照）；
4. Process vs outcome supervision、test-time compute；
5. Tool calling、agent loop、context management；
6. Multimodal 表征（survey 深度）；
7. Python/PyTorch 实验能力；
8. 每周时间与目标。

要求：
- 每次只问一个主要问题；
- 每题按七个维度评分并写入 state/answer-history.csv；
- 未验证知识保持 mastery=0；
- 采访后更新 current.md 和 mastery.md；
- 诊断结束后直接进入 A1，并配一张 decoder 数据流图；
- 不自动 Git commit；
- 展示修改摘要。
```
